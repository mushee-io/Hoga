import Link from 'next/link';
export default function Developers(){return <main className="page-width content"><p className="eyebrow">HOGA CONNECT / V1</p><h1>Build on the business runtime.</h1><p className="muted">API keys are issued by a business owner. All API calls use the same quotes, orders, payments, and fulfillment records as Hoga Web.</p><div className="panel"><h2>Getting started</h2><ol><li>Create an API key in your business developer dashboard.</li><li>Send it as <code>Authorization: Bearer hoga_live_…</code>.</li><li>Fetch <code>GET /api/v1/business</code> to retrieve the public business and services.</li><li>Create a session, request a quote, then retrieve the resulting order.</li></ol></div><div className="panel"><h2>Endpoints</h2><p><code>GET /api/v1/business</code> · public business configuration and services</p><p><code>POST /api/v1/sessions</code> · creates an API-channel conversation for an existing Hoga customer</p><p><code>POST /api/v1/quotes</code> · scopes and quotes a request using server-side pricing rules</p><p><code>POST /api/v1/quotes/accept</code> · accepts an open quote for its customer</p><p><code>GET /api/v1/conversations/:id</code> · conversation and quote history</p><p><code>GET /api/v1/orders/:id</code> · order, payment, fulfillment, and authorized deliverable</p></div><div className="panel"><h2>Webhooks & demo mode</h2><p>Hoga queues subscribed events including quote, payment, order, and fulfillment lifecycle changes. V1 persists delivery attempts; outbound signing and retry dispatch await encrypted endpoint-secret storage.</p><p>In demo mode, payments and AI output are visibly simulated. It never indicates that funds moved or a live Moove payment settled.</p></div><Link className="button" href="/signin?next=/dashboard/developer">Manage developer access ↗</Link></main>}
