import Link from 'next/link';
import {ArrowUpRight} from 'lucide-react';
export const categories=['Research','Development','Design','Marketing','Data','Writing','Blockchain','Business','Operations'];
export function Badge({children}:{children:React.ReactNode}){return <span className="badge">{children}</span>;}
export function Empty({title,children}:{title:string;children?:React.ReactNode}){return <div className="empty"><span className="empty-mark">↗</span><h3>{title}</h3>{children&&<p>{children}</p>}</div>;}
export function Heading({eyebrow,title,description,action}:{eyebrow?:string;title:string;description?:string;action?:React.ReactNode}){return <div className="heading"><div>{eyebrow&&<p className="eyebrow">{eyebrow}</p>}<h1>{title}</h1>{description&&<p className="muted">{description}</p>}</div>{action}</div>;}
export function Money({amount,currency}:{amount:{toString():string};currency:string}){return <span className="money">{Number(amount.toString()).toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:6})} <small>{currency}</small></span>;}
export function BusinessCard({business:b}:{business:{name:string;slug:string;logo:string;category:string;sample:boolean;shortDescription:string;currency:string;services:{name:string;price:{toString():string}}[]}}){return <Link href={`/b/${b.slug}`} className="business-card"><div className="row spread"><div className="avatar">{b.logo||b.name.slice(0,2)}</div><ArrowUpRight size={20}/></div><div><p className="eyebrow">{b.category}</p><h3>{b.name}</h3><p className="muted">{b.shortDescription}</p></div><div className="tags">{b.services.slice(0,2).map(s=><span key={s.name}>{s.name}</span>)}</div><div className="row spread card-bottom">{b.services[0]?<span>From <Money amount={b.services[0].price} currency={b.currency}/></span>:<span>No services yet</span>}{b.sample&&<Badge>Sample Business</Badge>}</div></Link>;}
