import {z} from 'zod';
export const money=z.string().regex(/^\d{1,10}(\.\d{1,6})?$/,'Use a positive amount with up to six decimal places');
export const businessInput=z.object({id:z.string().optional(),name:z.string().min(2).max(80),slug:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).min(3).max(60),logo:z.string().max(8),category:z.enum(['Research','Development','Design','Marketing','Data','Writing','Blockchain','Business','Operations']),shortDescription:z.string().min(10).max(180),description:z.string().min(10).max(5000),agentName:z.string().min(1).max(60),personality:z.string().max(1000),instructions:z.string().min(10).max(8000),fulfillmentInstructions:z.string().min(10).max(8000),currency:z.enum(['USDC','USD','EUR','GBP']),published:z.boolean()});
export const serviceInput=z.object({id:z.string().optional(),businessId:z.string(),name:z.string().min(2).max(100),description:z.string().min(10).max(3000),price:money,minimum:money,maxDiscount:z.coerce.number().int().min(0).max(100),negotiation:z.boolean(),startingPrice:z.boolean(),active:z.boolean()});
